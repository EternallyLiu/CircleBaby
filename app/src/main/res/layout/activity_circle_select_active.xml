<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/bg_color3"
    >

    <include
        android:id="@+id/topBar"
        layout="@layout/head"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/content_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/topBar"/>

    <Button
        android:id="@+id/btn_unjoin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="@dimen/size_60"
        android:layout_marginRight="@dimen/size_14"
        android:background="@drawable/select_active_unjoin_bg"
        android:paddingBottom="@dimen/size_4"
        android:paddingLeft="@dimen/size_24"
        android:paddingRight="@dimen/size_24"
        android:paddingTop="@dimen/size_4"
        android:text="@string/unjoin_active_tip"
        android:textColor="@color/white"
        android:textSize="@dimen/text_normal"/>

</RelativeLayout>